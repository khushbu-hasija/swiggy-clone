import RestaurantList from "./RestaurantList";

const Body = () => {
  return (
    <div className="body">
      <div className="search">Search</div>
      <RestaurantList />
    </div>
  );
};

export default Body;
